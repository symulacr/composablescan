# ComposableScan env
# Copy to .env.local and edit.

# Network
NEXT_PUBLIC_NETWORK=mainnet

# Mainnet API
NEXT_PUBLIC_MAINNET_API_BASE_URL=https://query.main.net.espresso.network
NEXT_PUBLIC_MAINNET_API_VERSION=v0

# WebSocket
NEXT_PUBLIC_MAINNET_WS_BASE_URL=wss://query.main.net.espresso.network

# Scan integration
NEXT_PUBLIC_MAINNET_SCAN_BASE_URL=https://explorer.main.net.espresso.network

# Rollup metadata
NEXT_PUBLIC_MAINNET_WEB_WORKER_URL=https://explorer.main.net.espresso.network/assets/node_validator_web_worker_api.js-bT9djMJi.js

# Environment
NODE_ENV=production

# Optional overrides (uncomment to override defaults)
# API timeout ms (default 10000)
# NEXT_PUBLIC_API_TIMEOUT=10000
# WS reconnects (default 5)
# NEXT_PUBLIC_WS_MAX_RECONNECTS=5
# Search debounce ms (default 300)
# NEXT_PUBLIC_SEARCH_DEBOUNCE=300
# Stats refresh ms (default 30000)
# NEXT_PUBLIC_NETWORK_STATS_REFRESH_MS=30000

# Deployment
# Prod: use production endpoints, NODE_ENV=production, CORS/rate limit
# Dev: copy to .env.local, NODE_ENV=development, testnet endpoints if needed

# Security
# NEXT_PUBLIC_* are public
# Do not put secrets in NEXT_PUBLIC_*
# Read-only app; no auth
