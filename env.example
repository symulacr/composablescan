# ComposableScan Environment Configuration
# Copy to .env.local and customize as needed

# ===========================================
# NETWORK CONFIGURATION
# ===========================================

# Primary network for the scan (always mainnet for this deployment)
NEXT_PUBLIC_NETWORK=mainnet

# ===========================================
# MAINNET API CONFIGURATION  
# ===========================================

# Espresso Network Mainnet API endpoint
NEXT_PUBLIC_MAINNET_API_BASE_URL=https://query.main.net.espresso.network

# API version for mainnet endpoints
NEXT_PUBLIC_MAINNET_API_VERSION=v0

# ===========================================
# WEBSOCKET CONFIGURATION
# ===========================================

# WebSocket endpoint for real-time block streaming
NEXT_PUBLIC_MAINNET_WS_BASE_URL=wss://query.main.net.espresso.network

# ===========================================
# SCAN INTEGRATION
# ===========================================

# Official Espresso Scan URL for deep links
NEXT_PUBLIC_MAINNET_SCAN_BASE_URL=https://explorer.main.net.espresso.network

# ===========================================
# ROLLUP METADATA CONFIGURATION
# ===========================================

# Web worker URL for rollup metadata (contains namespace mappings)
NEXT_PUBLIC_MAINNET_WEB_WORKER_URL=https://explorer.main.net.espresso.network/assets/node_validator_web_worker_api.js-bT9djMJi.js

# ===========================================
# DEVELOPMENT SETTINGS
# ===========================================

# Set to 'development' for additional logging and debugging features
NODE_ENV=production

# ===========================================
# OPTIONAL OVERRIDES
# ===========================================

# Uncomment and customize these variables to override default behavior:

# Custom API timeout in milliseconds (default: 10000)
# NEXT_PUBLIC_API_TIMEOUT=10000

# Custom WebSocket reconnection attempts (default: 5)  
# NEXT_PUBLIC_WS_MAX_RECONNECTS=5

# Custom search debounce delay in milliseconds (default: 300)
# NEXT_PUBLIC_SEARCH_DEBOUNCE=300

# ===========================================
# DEPLOYMENT NOTES
# ===========================================

# For production deployment:
# 1. Copy this file to .env.local 
# 2. Ensure all URLs point to production endpoints
# 3. Set NODE_ENV=production
# 4. Add domain-specific CORS configurations if needed
# 5. Consider rate limiting and caching strategies

# For development:
# 1. Copy this file to .env.local
# 2. Set NODE_ENV=development for additional debugging
# 3. Point to testnet endpoints if testing new features

# Security Notes:
# - All NEXT_PUBLIC_ variables are exposed to the browser
# - Never put sensitive API keys or secrets in NEXT_PUBLIC_ variables
# - This scan is read-only and doesn't require authentication